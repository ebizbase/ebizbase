name: Pull Request

on:
  pull_request_target:
    branches:
      - main
      - dev

jobs:

  lint:
    name: Linting
    uses: ./.github/workflows/lint.yaml

  test:
    name: Testing
    uses: ./.github/workflows/test.yaml

  build:
    name: Building
    uses: ./.github/workflows/build.yaml

