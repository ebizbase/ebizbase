# common-translator-marker

## Lint

Run `nx lint common-translator-marker` to build the library.

## Running unit tests

Run `nx test common-translator-marker` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build common-translator-marker` to build the library.
