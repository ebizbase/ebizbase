# nest-grpc-logger

## Lint

Run `nx lint nest-grpc-logger` to build the library.

## Running unit tests

Run `nx test nest-grpc-logger` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build nest-grpc-logger` to build the library.
