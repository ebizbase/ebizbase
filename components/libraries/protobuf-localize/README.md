# protobuf-localize

## Lint

Run `nx lint protobuf-localize` to build the library.

## Running unit tests

Run `nx test protobuf-localize` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build protobuf-localize` to build the library.
