# common-types

## Lint

Run `nx lint common-types` to build the library.

## Running unit tests

Run `nx test common-types` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build common-types` to build the library.
