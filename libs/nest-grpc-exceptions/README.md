# nest-grpc-exceptions

## Lint

Run `nx lint nest-grpc-exceptions` to build the library.

## Running unit tests

Run `nx test nest-grpc-exceptions` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build nest-grpc-exceptions` to build the library.
