# nest-grpc-testing

## Lint

Run `nx lint nest-grpc-testing` to build the library.

## Running unit tests

Run `nx test nest-grpc-testing` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build nest-grpc-testing` to build the library.
