# nest-grpc

## Lint

Run `nx lint nest-grpc` to build the library.

## Running unit tests

Run `nx test nest-grpc` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build nest-grpc` to build the library.
