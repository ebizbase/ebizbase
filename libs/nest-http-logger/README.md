# nest-http-logger

## Lint

Run `nx lint nest-http-logger` to build the library.

## Running unit tests

Run `nx test nest-http-logger` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build nest-http-logger` to build the library.
