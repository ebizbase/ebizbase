# nest-translator

## Lint

Run `nx lint nest-translator` to build the library.

## Running unit tests

Run `nx test nest-translator` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build nest-translator` to build the library.
