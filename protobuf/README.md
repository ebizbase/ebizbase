# protobuf

## Lint

Run `nx lint protobuf` to build the library.

## Running unit tests

Run `nx test protobuf` to execute the unit tests via [Jest](https://jestjs.io).

## Building

Run `nx build protobuf` to build the library.
